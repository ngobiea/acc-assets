'use server';
export const forgetPassword = async (useFormState: {}, formData: FormData) => {
    try {
        const email = formData.get('email');
    } catch (error) {
        
    }
    return {};
}